namespace Erp.Domain.Enums.Sys;

/// <summary>
/// Data scope 4 tầng (tham chiếu Digi ScopeType).
/// Own → Team → Department → All.
/// </summary>
public enum ScopeType
{
    Own = 1,
    Team = 2,
    Department = 3,
    All = 4
}
